<IfModule mod_rewrite.c>
    RewriteEngine On

    # Verhindert den Zugriff über den Browser (HTTP)
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteCond %{HTTP_USER_AGENT} !^$
    RewriteRule ^ - [F]
</IfModule>
